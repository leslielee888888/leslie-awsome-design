{
  "name": "design-system",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@12.5.1",
  "scripts": {
    "test": "pnpm -r run test",
    "validate": "pnpm -r run validate --if-present"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
