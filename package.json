{
  "name": "design-system",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "pnpm -r run test",
    "validate": "pnpm -r run validate"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
